#
# Docker Compose environment template
# Copy to .env and fill required values.
#

# Required
OPENAI_API_KEY=sk-your-key-here

# Optional Postgres overrides (docker-compose.yml provides sane defaults)
POSTGRES_DB=openai_batch_manager
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Optional: override app DB connection used by Docker Compose app container.
# Default points at the bundled postgres service in docker-compose.yml.
# DATABASE_URL=ecto://postgres:postgres@postgres:5432/openai_batch_manager

# Optional: local dev DB URL when running `mix phx.server` outside Docker.
# config/dev.exs reads DATABASE_URL_DEV.
DATABASE_URL_DEV=ecto://postgres:postgres@localhost:5432/openai_batch_manager_dev

# Optional RabbitMQ settings
# Used only if you uncomment the RabbitMQ env lines/service in docker-compose.yml
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
RABBITMQ_INPUT_QUEUE=batch_requests
